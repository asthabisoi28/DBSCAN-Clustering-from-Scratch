# DBSCAN-Clustering-from-Scratch
Implemented the DBSCAN clustering algorithm from scratch using Python and NumPy. The project generates a 2D dataset, identifies dense clusters and noise points using Euclidean distance, and visualizes the results using matplotlib.
